# Let NetworkManager manage all devices on this system
network:
  version: 2
  renderer: NetworkManager
  wifis:
    wlan0:
      dhcp4: no
      addresses:
        - 192.168.200.{{ id }}/24
      access-points:
        "robotont-{{ id }}":
          password: "{{ wifi_password }}"
          mode: ap
