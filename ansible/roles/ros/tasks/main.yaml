---
# Main tasks for the ROS role
- name: Set locale for ROS installation
  include_tasks: ros-locale.yaml
  tags: ros-locale

- name: Install ROS
  include_tasks: ros-install.yaml
  tags: ros-install

- name: ROS Packages Install
  include_tasks: ros-packages-install.yaml
  tags: ros-packages-install

- name: ROS Workspace Setup
  include_tasks: ros-workspace-setup.yaml
  tags: ros-workspace-setup